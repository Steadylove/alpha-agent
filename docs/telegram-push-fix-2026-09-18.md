# 买卖点推送修复记录：2026-09-18

## 问题与修复

1. **CVD、成交分布缺失**：昨天8条新版买点原始数据都没有 `volumeSnapshot`。两份专用 Pine 脚本已有分钟数据采集，本次补上告警协议 `signalProtocol: 2` 与版本 `aa-volume-v2-20260918`，方便识别旧告警。服务端记录“旧版未传 / 未采集 / 校验不通过”的原因；缺项仍不补估分。
2. **排名过期**：行情更新流程现在用最新已收盘日重新计算标普基准分位与个股 RPS，不再复制旧分位充当新一天的数据。标普有效样本必须覆盖至少98%，不足则停止发布。
3. **日线漏更新**：实际发现577只股票日线缺9月17日。原 Yahoo 请求可能成功但没有最新一天；现在有 Alpaca 凭据时优先使用现有 SIP 行情，检查日线是否真的补到目标日期。目标日期结合真实交易日历，并预留接口的20分钟延迟。
4. **永久缓存**：线上优先使用行情服务器，缓存60秒；刷新失败不续用过期缓存，也不退回部署时的旧文件。移除构建时打包排名的做法。
5. **时点与展示**：盘中信号使用前一交易日日线排名，记录 `asOf`、`generatedAt` 和基准。卡片显示排名日期、缺失项数量；“美东收盘信号”改为准确的“K线收盘信号”。无有效排名时买点不通过闸门，卖点继续转发。
6. **历史记录**：已保存的入场评分及来源保持不变；不把今日排名写回昨天的信号。

## 服务器数据修复

北京时间9月18日14:57，在独立目录试算后，持有每日任务锁完成备份和发布：

- 577份日线 CSV 仅追加9月17日。逐文件验证旧内容是新文件完整前缀。
- 最新排名截至9月17日，标普有效样本497/503，信号标的560只。
- 仅更新当日排名标尺，校验此前日期的分位和样本数未改变。
- 更新行情版本清单，生产HTTP读取返回200，并验证新排名。
- 备份：服务器 `/var/lib/alpha-agent/research/signal-fix-20260918/production-backup/`。
- 未调用推送任务，未重发或修改 Telegram / Discord 消息。

首次代码修复已提交为 `d108707` 并推送，Vercel 已报告部署成功。部署检查发现6只新增标普成分缺文件，因此将基准成分股的自动回补限定为排名所需的日线，盘中行情仍只更新原有交易池。

## TradingView 由用户配置

使用对应周期的完整文件：

- `docs/tradingview-signal-2h.pine`：2小时、常规交易时段。
- `docs/tradingview-signal-4h.pine`：4小时、常规交易时段。

更新指标后**重建已有告警**，条件选择本指标的 `Any alert() function call`，沿用现有 Webhook URL。只改图表代码不会更新旧告警保存的脚本副本。版本标记相同不代表分钟数据一定齐全；服务端仍检查近20根父级K线的分钟成交量覆盖率及时间、价格一致性。数据不足时继续显示缺项。

Pine 检查覆盖两份专用脚本与通用脚本的采集段一致性、买卖两种 payload 字段、服务端解析及评分契约。未在 TradingView 编译器中执行；配置时仍需确认编辑器编译成功。等待真实的新信号才能最终确认线上分钟数据已传入。

## 验证

- 14个相关测试文件，116项通过：包括交易日/节假日、延迟行情、缺最新日拒绝排名、未来数据不影响当日排名、远程缓存刷新失败不回退、旧排名拦截、卖点转发、入场评分不可覆盖、Pine载荷契约、评分和图片布局。
- TypeScript类型检查、变更文件ESLint与 `git diff --check` 通过。
- Next.js生产构建通过。首次本地沙箱限制了Turbopack内部端口，解除该构建的环境限制后成功；构建有一条动态文件追踪警告（`liveBook`），不影响本次构建。
- 在服务器直接运行新代码，只读调用 Alpaca 日线与行情HTTP服务，确认能取得9月17日MSFT日线、通过对应RPS时效校验。
- 用真实MSFT历史样例生成卡片，目视核对排名日期及“缺2项”；明确标为历史布局样例，未作为新信号发送。
